language: python
python:
  - 2.6
  - 2.7

# install requirments
install:
 - pip install argparse
 - pip install python-ldap
 - pip install psycopg2
 - pip install M2Crypto

# run tests
script:
 - py.test ovirt_engine_kerbldap_migration/tests/

# notifications
notifications:
  email: false
